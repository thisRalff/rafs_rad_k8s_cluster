# Secrets / credentials - never commit
aws_keys/
*.pem
*.key
credentials
.env
.env.*
!.env.example
*.env
!*.env.example
kubeconfig
kubeconfig-*

# Site-specific Docker build context — contains paid plugins, themes, and mu-plugins
/docker/

# Resumes - personal, don't commit
Rafael_Solo*.docx

# Notes and TODO - keep local
TODO.md
notes/

# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
!*.tfvars.example
*.tfplan
*.tfplan.*
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# OS
.DS_Store
